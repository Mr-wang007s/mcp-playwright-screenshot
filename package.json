{
  "name": "screenshot_url",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "MCP server: capture webpage screenshots via Playwright and return saved file path with domain safety guards.",
  "bin": {
    "screenshot_url": "dist/server.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node dist/server.js",
    "dev": "node --loader ts-node/esm src/server.ts"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^0.6.0",
    "zod-to-json-schema": "^3.23.5",
    "playwright": "^1.45.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^20.11.30",
    "ts-node": "^10.9.2",
    "typescript": "^5.4.5"
  }
}